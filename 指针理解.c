/**
 * @file    函数指针与回调机制.c
 * @brief   函数指针核心概念总结
 *
 * ## 核心直觉
 *
 * 指针变量和函数名，本质上是同一套逻辑：
 *   - 指针变量名  → 存储的是目标的首地址
 *   - 函数名      → 函数代码在 Flash 中的首地址
 *   - 数组名      → 数组第一个元素的首地址
 *
 * ## 指针三层模型
 *
 *   &cb  → cb 自己作为变量住在内存里的地址（不变）
 *   cb   → cb 里存的值（目标地址，可修改）
 *   *cb  → 去目标地址读/写内容
 *
 * ## 函数指针
 *
 *   typedef void (*Handler_t)(void);
 *   Handler_t cb = led;   // cb 里存的是 led 的入口地址
 *   cb();                 // 跳到该地址执行代码
 *
 *   cb  和 led 都是地址，可以画等号
 *   &cb 是 cb 这个变量自己的地址，和 led 的地址无关
 *
 * ## 回调机制
 *
 *   调用方把函数地址传进来（cb = led）
 *   驱动层保存这个地址（_rx_callback = cb）
 *   中断触发时跳过去执行（_rx_callback()）
 *   驱动层不关心上层怎么处理，实现彻底解耦
 *
 * ## 和数组的类比
 *
 *   arr[0]  → 去首地址取数据
 *   led()   → 去首地址执行代码
 *   本质相同，只是一个读数据，一个跑代码
 *
 * @author  Lin
 * @date    2026-05-11
 */